---
title: Registry MCP
description: Skillist exposes a public Model Context Protocol server for registry discovery and install help.
---

import { DocAlert } from "@/components/docs/DocAlert";
import { DocTable } from "@/components/docs/DocTable";
import { SectionHeading } from "@/components/docs/SectionHeading";

The **Skillist Registry MCP** server lets agents search, browse, and install skills without leaving the chat. It runs on the same API worker as the REST registry.

<DocAlert title="Endpoint" client:load>
  `https://api.skillist.dev/mcp` — streamable HTTP with optional OAuth.
</DocAlert>

<SectionHeading title="Transport" client:load />

<DocTable
  client:load
  columns={["Feature", "Value"]}
  rows={[
    ["Protocol", "MCP 2024-11-05"],
    ["Transport", "Streamable HTTP"],
    ["Stateless JSON", "POST with Accept: application/json"],
    ["SSE sessions", "POST/GET with Accept: text/event-stream and Mcp-Session-Id"],
    ["Session teardown", "DELETE with Mcp-Session-Id"],
  ]}
/>

`initialize` returns an `Mcp-Session-Id` header for stateful clients. Plain JSON-RPC POST remains supported for simple clients.

## Server info

```bash
curl https://api.skillist.dev/mcp
```

Returns transport metadata, tool names, and OAuth discovery URLs.

<SectionHeading title="Tools" client:load />

<DocTable
  client:load
  columns={["Tool", "Purpose"]}
  rows={[
    ["registry_search", "Search by query, category, tag, agent, sort"],
    ["registry_get_skill", "Full metadata for org/repo including eval uplift"],
    ["registry_facets", "Categories, tags, and compatible agents"],
    ["registry_install_help", "CLI install commands and SKILL.md URL"],
  ]}
/>

See [Tools reference](/mcp/tools/) for parameters and examples.

## Read-only by design

Registry MCP is read-only. Publish, edit, and sandbox **run** require the [CLI](/getting-started/cli/) with an authenticated API key.

## Next steps

- [Connect your agent](/mcp/connect/) — Cursor, Claude, VS Code
- [OAuth authentication](/mcp/oauth/) — sign in from OAuth-capable MCP clients
- [Delivery URLs](/platform/delivery/) — apex `skillist.dev/{org}/{repo}` paths returned by install help
